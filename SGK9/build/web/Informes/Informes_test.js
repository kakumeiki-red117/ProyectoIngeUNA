'use strict';

describe('myApp.Informes module', function() {

  beforeEach(module('myApp.Informes'));

  describe('Informes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var InformesCtrl = $controller('InformesCtrl');
      expect(InformesCtrl).toBeDefined();
    }));

  });
});