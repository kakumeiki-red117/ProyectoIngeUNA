'use strict';

describe('myApp.CreAct module', function() {

  beforeEach(module('myApp.CreAct'));

  describe('CreAct controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CreActCtrl = $controller('CreActCtrl');
      expect(CreActCtrl).toBeDefined();
    }));

  });
});