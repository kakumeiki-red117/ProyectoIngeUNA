'use strict';

describe('myApp.ConsInf module', function() {

  beforeEach(module('myApp.ConsInf'));

  describe('ConsInf controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ConsInfCtrl = $controller('ConsInfCtrl');
      expect(ConsInfCtrl).toBeDefined();
    }));

  });
});