'use strict';

describe('myApp.ConsAct module', function() {

  beforeEach(module('myApp.ConsAct'));

  describe('ConsAct controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ConsActCtrl = $controller('ConsActCtrl');
      expect(ConsActCtrl).toBeDefined();
    }));

  });
});